import Subject0003 from '@src/leetcode/0003';
describe('insert', () => {
  // 测试开始前
  beforeAll(async () => {});
  // 测试最后
  afterAll(async () => {});

  it('测试说明1', async () => {
    // Subject0003.read();
    // todo 期望断言
    // expect(x).toBe(1)
  });

  it('测试说明2', async () => {
    // todo 期望断言
    // expect(x).toBe(1)
  });
});
